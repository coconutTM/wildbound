diff --git a/project.godot b/project.godot
index c0a645e..1c1ee04 100644
--- a/project.godot
+++ b/project.godot
@@ -15,6 +15,11 @@ run/main_scene="uid://cuiu2lhryif6o"
 config/features=PackedStringArray("4.7", "GL Compatibility")
 config/icon="res://icon.svg"
 
+[autoload]
+
+SFX="*res://scripts/sfx_player.gd"
+Music="*res://scripts/music_player.gd"
+
 [display]
 
 window/size/viewport_width=1920
diff --git a/resources/stage_1.tres b/resources/stage_1.tres
index b96ecd5..a9837fb 100644
--- a/resources/stage_1.tres
+++ b/resources/stage_1.tres
@@ -7,10 +7,12 @@
 [ext_resource type="Resource" uid="uid://g8jj11q611b3" path="res://resources/wave_2.tres" id="4_4gp4j"]
 [ext_resource type="Resource" uid="uid://dfa55p0ct5rkq" path="res://resources/wave_3.tres" id="5_hnbuy"]
 [ext_resource type="Resource" uid="uid://dpxhquh6ikajy" path="res://resources/wave_4.tres" id="6_a3ss8"]
+[ext_resource type="AudioStream" uid="uid://7brdikkwoj1o" path="res://assets/sounds/songs/Coconut_Tree_Stand_off.mp3" id="7_music"]
 
 [resource]
 resource_local_to_scene = true
 script = ExtResource("1_dtus1")
 waves = Array[ExtResource("2_cnf0e")]([ExtResource("3_hpq3v"), ExtResource("4_4gp4j"), ExtResource("5_hnbuy"), ExtResource("6_a3ss8")])
 map_scene = ExtResource("1_hnbuy")
+music = ExtResource("7_music")
 metadata/_custom_type_script = "uid://bbovnix7vn37i"
diff --git a/resources/stage_2.tres b/resources/stage_2.tres
index d364cc8..bdf02d9 100644
--- a/resources/stage_2.tres
+++ b/resources/stage_2.tres
@@ -7,10 +7,12 @@
 [ext_resource type="Resource" uid="uid://g8jj11q611b3" path="res://resources/wave_2.tres" id="4_hgiri"]
 [ext_resource type="Resource" uid="uid://dfa55p0ct5rkq" path="res://resources/wave_3.tres" id="5_nr0b1"]
 [ext_resource type="Resource" uid="uid://dpxhquh6ikajy" path="res://resources/wave_4.tres" id="6_yp4ny"]
+[ext_resource type="AudioStream" uid="uid://nu502r4e7jc4" path="res://assets/sounds/songs/Beneath_the_Overgrowth.mp3" id="7_music"]
 
 [resource]
 resource_local_to_scene = true
 script = ExtResource("1_fpmad")
 waves = Array[ExtResource("2_ldiit")]([ExtResource("3_qhgde"), ExtResource("4_hgiri"), ExtResource("5_nr0b1"), ExtResource("6_yp4ny")])
 map_scene = ExtResource("1_hgiri")
+music = ExtResource("7_music")
 metadata/_custom_type_script = "uid://bbovnix7vn37i"
diff --git a/scripts/chest.gd b/scripts/chest.gd
index b5fb341..e64e159 100644
--- a/scripts/chest.gd
+++ b/scripts/chest.gd
@@ -32,6 +32,7 @@ func open() -> void:
 	if opened:
 		return
 	opened = true
+	SFX.play("chest_open")
 	drop_loot()
 	queue_free()  # ถ้าอยากให้กล่องค้างอยู่ในฉากแบบ "เปิดแล้ว" ให้เปลี่ยนเป็นสลับ sprite แทน queue_free()
 
diff --git a/scripts/craft_manager.gd b/scripts/craft_manager.gd
index b35703d..6fcbd3c 100644
--- a/scripts/craft_manager.gd
+++ b/scripts/craft_manager.gd
@@ -19,4 +19,5 @@ func craft(recipe: Weapon_Recipe) -> bool:
 		player.remove_component(component_type, recipe.cost[component_type])
 
 	player.equip_weapon(recipe.weapon_stats)
+	SFX.play("craft_success")
 	return true
diff --git a/scripts/credits.gd b/scripts/credits.gd
index 06a79de..365fe28 100644
--- a/scripts/credits.gd
+++ b/scripts/credits.gd
@@ -6,4 +6,5 @@ func _ready() -> void:
 
 
 func _on_btn_back_to_menu_pressed() -> void:
+	SFX.play("ui_click")
 	get_tree().change_scene_to_file("res://scenes/Menu.tscn")
diff --git a/scripts/enemy.gd b/scripts/enemy.gd
index f360cf3..3327d27 100644
--- a/scripts/enemy.gd
+++ b/scripts/enemy.gd
@@ -69,6 +69,7 @@ func handle_attack() -> void:
 func attack() -> void:
 	can_attack = false
 	attack_hitbox.activate()
+	SFX.play("attack_swing", -6.0)
 
 	await get_tree().create_timer(0.15).timeout  # ระยะเวลาที่ hitbox เปิดอยู่ ปรับได้ตามฟีล
 
@@ -82,10 +83,12 @@ func _on_attack_cooldown_timeout() -> void:
 
 func take_damage(amount: int) -> void:
 	stats.take_damage(amount)
+	SFX.play("hit_impact")
 
 # Milestone 1 แค่ต้องการให้ enemy หายไปเมื่อ HP หมด ("Enemy ตายได้")
 # ระบบ drop loot จะเพิ่มใน Milestone 3 (loot.gd) ไม่ต้องทำตอนนี้
 func die() -> void:
+	SFX.play("enemy_death")
 	died.emit()
 	queue_free()
 
diff --git a/scripts/game_manager.gd b/scripts/game_manager.gd
index 2dcf291..163da1d 100644
--- a/scripts/game_manager.gd
+++ b/scripts/game_manager.gd
@@ -29,12 +29,14 @@ func _on_player_died() -> void:
 func game_over() -> void:
 	state = State.GAME_OVER
 	game_over_triggered.emit()
+	Music.stop()
 	get_tree().change_scene_to_file.call_deferred("res://scenes/game_over.tscn")
 	#get_tree().quit()
 
 func win_game() -> void:
 	state = State.WIN
 	game_won.emit()
+	Music.stop()
 	get_tree().change_scene_to_file.call_deferred("res://scenes/win.tscn")
 
 # สำคัญ: reload_current_scene() จะรีเซ็ต node ทั้งหมดใหม่ (ตรงตาม spec
diff --git a/scripts/game_over.gd b/scripts/game_over.gd
index 06a79de..365fe28 100644
--- a/scripts/game_over.gd
+++ b/scripts/game_over.gd
@@ -6,4 +6,5 @@ func _ready() -> void:
 
 
 func _on_btn_back_to_menu_pressed() -> void:
+	SFX.play("ui_click")
 	get_tree().change_scene_to_file("res://scenes/Menu.tscn")
diff --git a/scripts/loot.gd b/scripts/loot.gd
index e474413..bc01078 100644
--- a/scripts/loot.gd
+++ b/scripts/loot.gd
@@ -17,6 +17,7 @@ func _on_body_entered(body: Node2D) -> void:
 		return
 
 	body.add_component(component_type, amount)
+	SFX.play("pickup_loot")
 	pickup()
 
 func pickup() -> void:
diff --git a/scripts/menu.gd b/scripts/menu.gd
index dcced0c..de61216 100644
--- a/scripts/menu.gd
+++ b/scripts/menu.gd
@@ -5,14 +5,17 @@ func _ready() -> void:
 	$UI.size = get_viewport_rect().size
 
 func _on_btn_play_pressed() -> void:
+	SFX.play("ui_click")
 	DisplayServer.window_set_mode(DisplayServer.WINDOW_MODE_WINDOWED)
 	get_tree().change_scene_to_file("res://scenes/main.tscn")
 
 func _on_btn_exit_pressed() -> void:
+	SFX.play("ui_click")
 	get_tree().quit()
 
 
 func _on_btn_credits_pressed() -> void:
+	SFX.play("ui_click")
 	get_tree().change_scene_to_file("res://scenes/credits.tscn")
 
 
diff --git a/scripts/music_player.gd b/scripts/music_player.gd
new file mode 100644
index 0000000..5447bf8
--- /dev/null
+++ b/scripts/music_player.gd
@@ -0,0 +1,43 @@
+extends Node
+
+# Autoload (singleton) - ต้อง register ใน Project Settings > Autoload ชื่อ "Music"
+# อยู่ข้ามทุก scene เพราะ autoload ไม่โดน free ตอน change_scene_to_file
+# เรียกใช้จากที่ไหนก็ได้: Music.play_track(some_stream), Music.stop()
+
+var player: AudioStreamPlayer
+var current_stream: AudioStream = null
+
+
+func _ready() -> void:
+	player = AudioStreamPlayer.new()
+	add_child(player)
+
+
+# ถ้า stream เดียวกับที่เล่นอยู่แล้ว จะไม่ restart ซ้ำ (กันเพลงกระตุกตอนเรียกซ้ำ)
+func play_track(stream: AudioStream, fade_in: float = 0.6) -> void:
+	if not stream:
+		return
+	if stream == current_stream and player.playing:
+		return
+
+	# ตั้ง loop = true ที่ตัว resource ตรง ๆ กันไม่ต้องยุ่งกับ import setting
+	if "loop" in stream:
+		stream.loop = true
+
+	current_stream = stream
+	player.stream = stream
+	player.volume_db = -80.0
+	player.play()
+
+	var tween := create_tween()
+	tween.tween_property(player, "volume_db", 0.0, fade_in)
+
+
+func stop(fade_out: float = 0.6) -> void:
+	if not player.playing:
+		return
+
+	current_stream = null
+	var tween := create_tween()
+	tween.tween_property(player, "volume_db", -80.0, fade_out)
+	tween.tween_callback(player.stop)
diff --git a/scripts/player.gd b/scripts/player.gd
index b8f56b0..fa71224 100644
--- a/scripts/player.gd
+++ b/scripts/player.gd
@@ -75,9 +75,11 @@ func handle_attack() -> void:
 
 func take_damage(amount: int) -> void:
 	stats.take_damage(amount)
+	SFX.play("player_hurt")
 
 func die() -> void:
 	print("U died noob")
+	SFX.play("player_death")
 	set_physics_process(false)
 	died.emit()
 
@@ -131,6 +133,7 @@ func attack() -> void:
 	weapon_sprite.visible = true
 	attack_hitbox.activate()
 	attack_animation.play("attack")
+	SFX.play("attack_swing")
 	
 	# ระยะเวลาที่ hitbox เปิดอยู่ ตั้งตาม animation "attack" ยาวประมาณ 1 วินาที
 	# ถ้าปรับความเร็ว/ความยาว animation ทีหลัง ต้องมาปรับเลขนี้ให้ตรงกันด้วย
diff --git a/scripts/sfx_player.gd b/scripts/sfx_player.gd
new file mode 100644
index 0000000..b1f8755
--- /dev/null
+++ b/scripts/sfx_player.gd
@@ -0,0 +1,45 @@
+extends Node
+
+# Autoload (singleton) เรียกใช้จากที่ไหนก็ได้ในเกมผ่าน SFX.play("ชื่อเสียง")
+# ต้อง register ใน Project Settings > Autoload ชื่อ "SFX" ชี้มาไฟล์นี้
+# (ดูขั้นตอนละเอียดในคอมเมนต์ท้ายไฟล์ project.godot)
+
+const SFX_DIR := "res://assets/sounds/effects/"
+const SFX_NAMES := [
+	"attack_swing",
+	"hit_impact",
+	"enemy_death",
+	"player_hurt",
+	"player_death",
+	"pickup_loot",
+	"chest_open",
+	"craft_success",
+	"wave_start",
+	"ui_click",
+]
+
+var streams: Dictionary = {}
+
+
+func _ready() -> void:
+	for sfx_name in SFX_NAMES:
+		var stream := load(SFX_DIR + sfx_name + ".wav") as AudioStream
+		if stream:
+			streams[sfx_name] = stream
+		else:
+			push_warning("SFX โหลดไม่เจอ: %s" % sfx_name)
+
+
+# เล่นเสียง one-shot ซ้อนกันได้หลายตัวพร้อมกัน (เช่น enemy โดนตีพร้อมกันหลายตัว)
+# ใช้ AudioStreamPlayer แยกตัวต่อครั้ง แล้ว queue_free ทิ้งเองตอนเล่นจบ
+func play(sfx_name: String, volume_db: float = 0.0) -> void:
+	if not streams.has(sfx_name):
+		push_warning("ไม่รู้จักชื่อ SFX: %s" % sfx_name)
+		return
+
+	var player := AudioStreamPlayer.new()
+	player.stream = streams[sfx_name]
+	player.volume_db = volume_db
+	add_child(player)
+	player.finished.connect(player.queue_free)
+	player.play()
diff --git a/scripts/stage_data.gd b/scripts/stage_data.gd
index bf591ab..c261df4 100644
--- a/scripts/stage_data.gd
+++ b/scripts/stage_data.gd
@@ -7,3 +7,6 @@ class_name Stage_Data extends Resource
 ## map scene ของ stage นี้ — ต้องมีลูกชื่อ "SpawnPoints" (มี Marker2D ข้างใน)
 ## และลูกชื่อ "PlayerStart" (Marker2D จุดเกิด player)
 @export var map_scene: PackedScene
+
+## เพลงประจำ stage นี้ เล่นวน ตอนเข้า stage (ไม่ใส่ก็ได้ ถ้าไม่ใส่จะไม่มีเพลงเปลี่ยน)
+@export var music: AudioStream
diff --git a/scripts/stage_manager.gd b/scripts/stage_manager.gd
index 51f84f3..a95ea8b 100644
--- a/scripts/stage_manager.gd
+++ b/scripts/stage_manager.gd
@@ -27,6 +27,8 @@ func start_stage() -> void:
 
 	_load_map(stages[current_stage_index])
 
+	Music.play_track(stages[current_stage_index].music)
+
 	wave_manager.waves = stages[current_stage_index].waves
 	wave_manager.current_wave_index = -1
 
diff --git a/scripts/wave_manager.gd b/scripts/wave_manager.gd
index 08242d1..fcb3ffb 100644
--- a/scripts/wave_manager.gd
+++ b/scripts/wave_manager.gd
@@ -23,6 +23,7 @@ func start_next_wave() -> void:
 		return
 
 	print("Wave %d started" % (current_wave_index + 1))
+	SFX.play("wave_start")
 	_spawn_wave(waves[current_wave_index])
 	wave_started.emit(current_wave_index + 1)
 
diff --git a/scripts/win.gd b/scripts/win.gd
index 06a79de..365fe28 100644
--- a/scripts/win.gd
+++ b/scripts/win.gd
@@ -6,4 +6,5 @@ func _ready() -> void:
 
 
 func _on_btn_back_to_menu_pressed() -> void:
+	SFX.play("ui_click")
 	get_tree().change_scene_to_file("res://scenes/Menu.tscn")
